import * as types from '../constants/actionTypes'

//example from mega markets to follow structure
// export const loadSlots = (slots) => ({
//     type: types.LOAD_SLOTS,
//     payload: slots,
//   });